#!/bin/sh
echo "Useful Calculators"
DEPLOY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
TOP_DIR=$( dirname "${DEPLOY_DIR}" )
/usr/bin/env python ${TOP_DIR}/usefulCalculators.py
